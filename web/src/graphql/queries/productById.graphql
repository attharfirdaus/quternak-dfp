query ProductById($id: Float!) {
    productById(id: $id){
        id
        title
        category{
            id
            name
        }
        description
        location
        pictureUrl
        stock
        price
        status
        type
        variant
        seller{
            id
            profilePictureUrl
            name
            phoneNumber
        }
        createdAt
        updatedAt
    }
}